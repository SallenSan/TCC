spring.application.name=consultas-psicologicas

# Configuração do banco
spring.datasource.url=jdbc:postgresql://localhost:5432/consultas
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Perfis
#spring.config.name=application
#spring.profiles.active=default

# Porta
server.port=8080

# Jackson - evitar erro em serialização
spring.jackson.serialization.fail-on-empty-beans=false

# Logs
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG

# DevTools (evita reinício quebrando conexão)
spring.devtools.restart.enabled=false

# Email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=consulta.teste.2025@gmail.com
spring.mail.password=hzjw rqbm ddje mioj
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
